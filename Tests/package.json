{
  "type": "module",
  "scripts": {
    "watchTS": "dotnet fable watch --outDir ts --lang typescript ../Src/Dicts.fsproj --run npx tsc" ,

    "buildTS": "dotnet fable --outDir ts --noCache  --lang typescript ../Src/Dicts.fsproj --silent --run npx tsc" ,
    "pretest": "dotnet fable --outDir js --noCache ",
    "test": "mocha js && npm run buildTS"
  },
  "dependencies": {
    "mocha": "12.0.0-beta-9.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
